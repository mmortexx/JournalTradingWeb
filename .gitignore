# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...salvo la plantilla, que documenta qué variables hacen falta.
!.env.example

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
# Anclados a la RAÍZ con la barra inicial, y no es un detalle de estilo.
# Sin ella, `test` ignora CUALQUIER fichero o carpeta con ese nombre a
# cualquier profundidad. Se llevó por delante `src/app/test/` —la página
# del diagnóstico— sin decir nada: el commit salió limpio, el despliegue
# en verde, y el sitio quedó publicado con un enlace en el menú a una
# página que no existía, porque el menú y el mapa del sitio SÍ viajaron y
# la página no. Un 404 servido con toda naturalidad.
#
# Estas dos entradas eran para ficheros sueltos de trabajo en la raíz; ahí
# se quedan.
/test
/prompt

server.log
# Skills directory
/skills/
# Temp screenshots / tool-results / agent context (not for repo)
screenshots/
tool-results/
agent-ctx/
.zscripts/dev.pid
--clip
r3*.png
*.png.bak
download/
upload/
db/custom.db

# Sandbox-only helpers (not part of the project)
.zscripts/run-dev.sh
.zscripts/dev.log
.zscripts/dev.pid
.zscripts/start-dev-persistent.sh

# fuentes que scripts/generate-og.py descarga y cachea (no se versionan)
/scripts/.fonts/
