{
  "name": "countpips-beta-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply countpips-beta --local",
    "db:migrate:remote": "wrangler d1 migrations apply countpips-beta --remote"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
